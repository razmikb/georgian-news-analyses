# Secrets — never commit
.env
.env.*
!.env.example

# Working docs & agent config — local only, not published in the public repo
CLAUDE.md
PLAN.md
PROGRESS.md
WORKFLOW.md
DESIGN.md
overall-goal.md
.claude/

# Local scratch cache — fetched article text and embedding vectors, kept only so
# re-running an experiment doesn't re-fetch 120 pages. Article text must never be
# committed (copyright, PLAN.md §9), so this line is load-bearing, not tidiness.
.cache/

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
.vercel/
dist/

# OS / editor
.DS_Store
Thumbs.db
